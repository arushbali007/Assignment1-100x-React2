# =============================================================================
# CreatorPulse Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual API keys and configuration

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Get these from https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=your-supabase-project-url
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-key

# =============================================================================
# APPLICATION SETTINGS (REQUIRED)
# =============================================================================
# Secret key for JWT token signing (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here-generate-random-string

# Application name (displayed in UI)
APP_NAME=CreatorPulse

# Environment: development, staging, or production
APP_ENV=development

# =============================================================================
# AI/LLM CONFIGURATION (REQUIRED)
# =============================================================================
# Groq Cloud API key for AI features (style analysis, draft generation)
# Get your key from https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here

# =============================================================================
# EMAIL DELIVERY (REQUIRED)
# =============================================================================
# Resend API key for sending newsletters
# Get your key from https://resend.com/api-keys
RESEND_API_KEY=your-resend-api-key-here

# Default sender email (use onboarding@resend.dev for testing or your verified domain)
RESEND_FROM_EMAIL=onboarding@resend.dev

# =============================================================================
# CONTENT SOURCE APIS (OPTIONAL)
# =============================================================================
# These are only required if you want to use specific source types
# You can use the app without these by only adding RSS feeds

# ---- YouTube API ----
# Get key from https://console.cloud.google.com/apis/credentials
# Required for: Adding YouTube channels as content sources
YOUTUBE_API_KEY=your-youtube-api-key

# ---- Twitter API v2 ----
# Get credentials from https://developer.twitter.com/en/portal/dashboard
# Required for: Adding Twitter accounts as content sources
TWITTER_BEARER_TOKEN=your-twitter-bearer-token
TWITTER_API_KEY=your-twitter-api-key
TWITTER_API_SECRET=your-twitter-api-secret

# =============================================================================
# SCHEDULER CONFIGURATION (OPTIONAL)
# =============================================================================
# These control when automated tasks run
# Default values are shown below - uncomment to customize

# Content fetch schedule (default: every 4 hours + 6 AM, 12 PM, 6 PM)
# CONTENT_FETCH_INTERVAL_HOURS=4

# Trend detection schedule (default: 7 AM and 7 PM)
# TREND_DETECTION_HOURS=7,19

# Draft generation schedule (default: 7 AM daily)
# DRAFT_GENERATION_HOUR=7

# Timezone for scheduler (default: UTC)
# SCHEDULER_TIMEZONE=UTC

# =============================================================================
# API CONFIGURATION (OPTIONAL)
# =============================================================================
# Backend API base URL (default: http://localhost:8000)
# API_BASE_URL=http://localhost:8000

# Frontend base URL (default: http://localhost:8501)
# FRONTEND_URL=http://localhost:8501

# =============================================================================
# CORS CONFIGURATION (OPTIONAL)
# =============================================================================
# Allowed origins for CORS (default: http://localhost:8501)
# CORS_ORIGINS=http://localhost:8501,http://localhost:3000

# =============================================================================
# LOGGING CONFIGURATION (OPTIONAL)
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# =============================================================================
# NOTES
# =============================================================================
# 1. NEVER commit the actual .env file to version control
# 2. Generate a strong SECRET_KEY: openssl rand -hex 32
# 3. Use onboarding@resend.dev for testing emails (no verification needed)
# 4. For production, verify your domain in Resend and use your own email
# 5. RSS feeds don't require any API keys
# 6. Google Trends is used automatically (no API key needed)
# 7. Run database migrations before starting: migrations/003_complete_schema.sql
